"""Supersonic — a Checkpoint/Verify/Rollback build loop with a structured
Continuity Graph memory.

Nothing here is a fork or reskin of a prior tool: the memory model and the
turn-loop safety mechanism are different algorithms, not a renamed version
of a token-eviction / single-call-router design.
"""

__version__ = "1.0.0"

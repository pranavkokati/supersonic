"""Optional shipping/notification integrations — none of them required to run the loop.

git_ops.py  native git commit/push/branch operations
github.py   native `gh` CLI repo creation + push/PR — no Composio middleman
linear.py   optional issue tracking, off unless linear_api_key is set
notify.py   optional completion webhook
"""
